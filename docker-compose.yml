version: '3.8'

services:
  eureka-scraper:
    build: .
    container_name: eureka-scraper
    env_file:
      - .env
    volumes:
      # Persist scraped data
      - ./data:/app/data
      # Persist outputs (logs, excel exports)
      - ./outputs:/app/outputs
    restart: unless-stopped
    # Run on a schedule using cron-like syntax
    # Uncomment and modify the command below to run on a schedule
    # command: >
    #   sh -c "
    #   while true; do
    #     python3 cron_job.py;
    #     sleep 86400;
    #   done
    #   "
